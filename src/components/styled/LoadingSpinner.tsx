import styled from "styled-components";

const LoadingSpinner = styled.div`
  height: 100vh;
  width: 100vw;
  & img {
    position: absolute;
    left: 50%;
    bottom: 0px;
  }
`;
export default LoadingSpinner;
